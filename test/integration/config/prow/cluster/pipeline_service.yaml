apiVersion: v1
kind: Service
metadata:
  name: pipeline
  namespace: default
  labels:
    app: pipeline
spec:
  selector:
    app: pipeline
  ports:
  - name: http
    port: 80
    targetPort: 8080
  - name: metrics
    port: 9090
    targetPort: 9090
  type: ClusterIP
